baseURL = "https://example.com/"
languageCode = "en-ca"
title = "New Site"
enableRobotsTXT = true

# If you're using Hugo Modules, this file becomes the canonical import list.
[module]
  [[module.imports]]
    path = "github.com/nunocoracao/blowfish/v2"

  # Shortcodes module (rename to the actual repo path once you rename it)
  [[module.imports]]
    path = "github.com/DilexNetworks/core-hugo-shortcodes/module"
    # If you want to pin versions later, do it via go.mod / hugo mod commands.

[params]
  description = "A Hugo starter site."
